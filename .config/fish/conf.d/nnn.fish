# here I define keybindings and respective plugins
#
# to call one of them I must hit ; with plugin key followed.
# I hit "; q"" to "fixname" of a file at the point
set -x NNN_PLUG 'h:gitroot;s:suedit;n:bulknew;q:fixname;o:organize;u:getplugs;p:preview-tabbed;x:xdgdefault;t:telegram-send'

# I don't know what is it
set -x NNN_FIFO /tmp/nnn.fifo
